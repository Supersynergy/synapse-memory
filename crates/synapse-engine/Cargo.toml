[package]
name = "synapse-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
license = "MIT"

[lib]
name = "synapse_engine"
crate-type = ["cdylib", "rlib"]

[dependencies]
obfstr = "0.4"
