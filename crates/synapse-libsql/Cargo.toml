[package]
name = "synapse-libsql"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "libSQL async-WAL backend for Synapse Store. Cluster-B of synapse-gap-sprint."

[dependencies]
async-trait = "0.1"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt"] }
# libsql = { version = "0.6", optional = true }  # TODO: enable for full backend

[features]
default = []
libsql-backend = []  # TODO: gate `libsql` dep + Connection wiring
