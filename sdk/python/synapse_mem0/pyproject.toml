[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "synapse-mem0"
version = "0.1.0"
description = "Drop-in mem0 replacement backed by synapse – local, fast, zero cloud"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = [
  "msgpack>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["."]
include = ["synapse_mem0*"]
