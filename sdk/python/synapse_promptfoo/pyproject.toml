[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "synapse-promptfoo"
version = "0.1.0"
description = "promptfoo eval provider for synapse RAG benchmarking"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Maxim Supersynergy", email = "true@supersynergy.de" }]
requires-python = ">=3.11"
dependencies = ["msgpack>=1.0"]
