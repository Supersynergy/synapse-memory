# SYNERGY 2026-04-25 — Architecture × Bench Coordination

Merge document between Session A (architectural research, 22 subagents) and
Session B (comprehensive bench harness, 5 real DBs). Append-only after this draft.

## 1. Don't-Touch Map (file ownership)

| Track | Files | Other side may edit? |
|---|---|---|
| Architecture (A) | `crates/synapse-core/`, `crates/synapse-mysql-async/`, `sql_fns.rs`, `embed_mlx.rs`, `Cargo.toml`, `Cargo.lock`, `MASTER-CHECK*.md`, `SPEED-INTEL*.md`, `MASTERPLAN*.md`, `PHASE-*.md` | NO |
| Bench (B) | `bench/comprehensive/*` (`bench.py`, `run.sh`, `setup.sh`, `report.py`, `dataset.parquet`, `results/`, `snapshots/`) | NO |
| Shared | `RESULTS_FAST.md` (read-only for A), `SYNERGY-2026-04-25.md` (this file, append-only) | both append, never overwrite |

Rule: if either side must edit a file in the other's column, add a `[REQUEST]`
entry under §6 Updates and wait for ack.

## 2. Architecture × Bench Phase Map

Baseline = Session B measurements on M4 Max, 10k docs, 384-dim, 2026-04-25.
Targets = Session A SPEED-INTEL-FINAL claims to validate.

| # | Change (Session A) | Expected gain | Validating bench phase | Baseline (B) | Target after change |
|---|---|---:|---|---:|---:|
| 1 | dashmap v5 replaces RwLock<BTreeMap> | 4× concurrent reads | D (8t select), F (sweep) | synapse 46,981 ops/s @8t | 180k ops/s @8t |
| 2 | Async group commit (FastFS) | 3-5× sustained writes | A (bulk insert), H (new) | synapse 47,050 ops/s | 140-235k ops/s |
| 3 | Shard-per-core read (tokio task_local) | 2-3× latency | D, F | p50 ≈ 0ms (sub-ms) | tail p99 cut 2-3× |
| 4 | libSQL BEGIN CONCURRENT | 4× write-mix | C (mixed 80/20) | synapse 283.6 ops/s | 1,100+ ops/s |
| 5 | FTS5 batch multi-row INSERT | 3-5× | A, H | 47,050 ops/s | 140-235k ops/s |
| 6 | MLX Metal embed batch | 10× (skip step) | G (new, Parquet load) | included in 47k | n/a — bypassed |
| 7 | Parquet pre-embed bulk load | 10× ingest | G (new) | 47k ops/s w/ embed | 470k–1M docs/s |
| 8 | SimSIMD RRF | rerank cost ↓ | D, E | recall 0.096 (broken) | recall ≥0.90 + same QPS |
| 9 | LRU embed + query cache | 2× | I (new) | n/a | hitrate ≥70% repeat |
| 10 | Request batching (10→1) | 10× over wire | mysql-async harness (out of scope here) | 1,078 sysbench QPS | 25-50k QPS @8t |
| 11 | Connection pool 64 (deadpool) | 3× | F | n/a | 5k → 15k QPS step |

Compound projection (Session A): 1,078 → 50k QPS @8t in 14d; 1M docs/s ingest in 2w.

## 3. Honest Ceiling vs Measured (M4 Max)

| Goal | Session A claim | Session B measured (10k docs) | Gap / status |
|---|---|---|---|
| Match MariaDB ~7.6k OPS @8t | feasible 14d | 1,078 QPS (sysbench TLS) | 7× gap, plan is pool+rayon+FTS5 batch |
| 50k QPS @8t | feasible 14d | sqlite-vec 55,943 / synapse 46,981 ops/s @8t | already met at this scale; need 100k+ docs stress |
| 100k QPS @8t | unlikely on M4 alone | not measured | needs Linux glommio + io_uring |
| 1M QPS @8t | expert-only 60+d | n/a | DPDK+mmap+LSM, out of scope |
| 100k docs/s ingest | feasible 1w | bulk insert 47k ops/s WITH embed | pragma + skip-embed |
| 1M docs/s pre-embedded ingest | feasible 2w | n/a (embed in critical path) | new Phase G needed |
| Recall@10 ≥ 0.90 | implicit | synapse 0.096, sqlite-vec 0.112 | **broken** — fix before any speed work counts |

Critical: recall@10 = 0.096 means current Phase E result is non-publishable. Session B
is fixing query/index alignment in-flight; Session A should not cite Phase E numbers
until recall ≥0.85.

## 4. New Bench Phases Needed

Session B will add to `bench/comprehensive/bench.py` (own column, no A coordination needed):

| Phase | Name | Validates A claim | Method |
|---|---|---|---|
| G | Parquet pre-embed bulk load | 1M docs/s ingest, MLX batch | DuckDB read parquet → INSERT, embed step skipped |
| H | Group-commit coalescing | 3-5× sustained writes | sustained INSERT loop with explicit batch sizes 1/100/1k/10k |
| I | Cache hit-rate | LRU 2× | repeat top-100 queries 1000× → measure T1 hit ratio |
| J | Saturation curve | glommio/io_uring breakeven | concurrency sweep 1/4/8/16/32/64 threads |
| K | TLS-strip A/B | sysbench gap | bench mysql-async with TLS off vs on, isolate 30ms TLS cost |

Phase F (concurrency sweep 1/4/8/16T) and Phase G (batch update sweep 1/100/1k) already
stubbed in `RESULTS_FAST.md` but unfilled — fill before adding J above.

## 5. Coordination Protocol

1. Both sessions **read this file** before any change touching the other's column.
2. Updates are **append-only** under §6 with timestamp + session id (A or B) +
   one-line summary. Never edit prior entries.
3. Edits to Don't-Touch files require a `[REQUEST]` line + ack from owner.
4. Numbers cited cross-session must link to source: `RESULTS_FAST.md§Phase X` or
   `SPEED-INTEL-FINAL§section`.
5. If A lands an optimization, A appends `[LANDED]` with commit SHA and the
   phase B should re-run. B re-runs and appends `[VERIFIED]` with delta.
6. Recall@10 is a **publish gate**. No speed claims published until ≥0.85 across
   synapse + sqlite-vec.

## 6. Updates Log

- 2026-04-25 [B] Initial draft. Bench at phases A-F filled, F/G stubs empty,
  recall fix in-flight (currently 0.096, target ≥0.85). Baselines from
  `bench/comprehensive/RESULTS_FAST.md`.
- 2026-04-25 [B] New phases G/H/I/J/K queued for `bench.py` next iteration.
- (append future entries below this line)
