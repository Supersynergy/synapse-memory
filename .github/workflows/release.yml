name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: synapse-macos-arm64
            synx_artifact: synx-aarch64-apple-darwin
          - target: x86_64-apple-darwin
            os: macos-13
            artifact: synapse-macos-x86_64
            synx_artifact: synx-x86_64-apple-darwin
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: synapse-linux-x86_64
            synx_artifact: synx-x86_64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.95
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.95"
          targets: ${{ matrix.target }}

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ matrix.target }}-cargo-

      - name: Build synapse CLI
        run: cargo build --release --target ${{ matrix.target }} -p synapse-cli

      - name: Build synapsed daemon
        run: cargo build --release --target ${{ matrix.target }} -p synapsed

      - name: Package synapse+synapsed
        run: |
          mkdir dist
          cp target/${{ matrix.target }}/release/synapse dist/
          cp target/${{ matrix.target }}/release/synapsed dist/
          tar czf ${{ matrix.artifact }}.tar.gz -C dist .

      - name: Package synx (CLI binary + completions)
        run: |
          mkdir synx-dist
          cp target/${{ matrix.target }}/release/synapse synx-dist/synx
          # Generate shell completions if supported
          mkdir -p synx-dist/completions
          ./synx-dist/synx completions bash > synx-dist/completions/synx.bash 2>/dev/null || true
          ./synx-dist/synx completions zsh  > synx-dist/completions/_synx    2>/dev/null || true
          ./synx-dist/synx completions fish > synx-dist/completions/synx.fish 2>/dev/null || true
          tar czf ${{ matrix.synx_artifact }}.tar.gz -C synx-dist .

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.synx_artifact }}
          path: ${{ matrix.synx_artifact }}.tar.gz

  release:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.tar.gz
          generate_release_notes: true
